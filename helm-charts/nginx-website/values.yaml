# Default values for nginx-website-chart
replicaCount: 6

image:
  repository: nginx
  pullPolicy: IfNotPresent
  tag: "stable"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP  # Kein LoadBalancer mehr - läuft über Ingress!
  port: 80

ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: k8s.sebastianmeyer.org
      paths:
        - path: /
          pathType: Prefix
    - host: www.k8s.sebastianmeyer.org
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: nginx-tls
      hosts:
        - k8s.sebastianmeyer.org
        - www.k8s.sebastianmeyer.org

website:
  language: "de"
  title: "Sebastian Meyer - Kubernetes HA Cluster"
  favicon: "🚀"
  fontFamily: "Arial"
  background: "linear-gradient(135deg, #667eea 0%, #764ba2 100%)"
  textColor: "white"
  accentColor: "#4CAF50"
  mainHeading: "🚀 Kubernetes HA Cluster"
  subtitle: "High-Availability Kubernetes mit NGINX & TLS"
  welcomeTitle: "Willkommen auf Sebastian Meyers Kubernetes Cluster"
  welcomeText: "Diese statische Website wird über ein hochverfügbares Kubernetes-Cluster bereitgestellt."
  techTitle: "Technologien"
  technologies: "Kubernetes, NGINX, Docker, TLS/SSL"
  infraTitle: "Infrastruktur"
  infrastructure: "High-Availability Setup mit Load Balancing"
  features:
    - icon: "🖥️"
      title: "Cluster Setup"
      description: "3-Node HA Cluster"
    - icon: "🌐"
      title: "Load Balancing"
      description: "NGINX Ingress"
    - icon: "🔐"
      title: "SSL/TLS"
      description: "Automatische HTTPS"
    - icon: "📦"
      title: "Container"
      description: "Docker & Kubernetes"
    - icon: "⚖️"
      title: "High Availability"
      description: "Redundante Services"
    - icon: "⚡"
      title: "Performance"
      description: "Optimiert & Skalierbar"

# Resource limits for better performance
resources:
  limits:
    cpu: 100m
    memory: 64Mi
  requests:
    cpu: 50m
    memory: 32Mi

# NGINX performance configuration
nginx:
  config: |
    server {
        listen 80;
        server_name _;
        root /usr/share/nginx/html;
        index index.html;
        
        # Gzip compression
        gzip on;
        gzip_vary on;
        gzip_min_length 1000;
        gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
        
        # Browser caching
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
            expires 1M;
            add_header Cache-Control "public, immutable";
        }
        
        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header X-Content-Type-Options "nosniff" always;
        
        # Keep alive
        keepalive_timeout 30;
        keepalive_requests 100;
        
        location / {
            try_files $uri $uri/ =404;
        }
    }
